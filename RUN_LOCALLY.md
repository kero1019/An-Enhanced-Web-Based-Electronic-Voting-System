# تشغيل مشروع التصويت محليًا

الفولدر ده هو الواجهة. فولدر `Voting_System` فيه السيرفر الجديد وقاعدة البيانات المحلية، وبجانبه كود NestJS/MySQL الأصلي.

## التشغيل

المطلوب **Node.js 24 أو أحدث**. النسخة الموجودة أثناء التطوير: 24.11.1.

افتح PowerShell داخل فولدر الواجهة أو فولدر الباك، ونفّذ نفس الأمر:

```powershell
npm.cmd run start:local
```

الأمر من فولدر الواجهة يشغّل الباك تلقائيًا ويستخدم نفس قاعدة البيانات. شغّله من فولدر واحد فقط، وسيب الـTerminal مفتوحة. لما تخلص اضغط **Ctrl+C** في نفس الـTerminal؛ ده يوقف السيرفر ويقفل بورت المشروع، والبيانات تفضل محفوظة. قفل صفحة المتصفح وحده لا يوقف السيرفر.

رابط GitHub Pages يعمل أيضًا بدون تشغيل السيرفر: `https://votingproject2023.github.io/An-Enhanced-Web-Based-Electronic-Voting-System/`. هناك يعمل التطبيق كوضع Demo داخل المتصفح؛ التسجيل، التصويت، والنتائج متاحة، لكن بيانات كل زائر محفوظة في متصفحه فقط ولا تنتقل لجهاز أو زائر آخر.

لو مكان فولدر الباك اتغير، حدد `VOTING_BACKEND_DIR` بالمسار الجديد قبل التشغيل من الواجهة. افتراضيًا الواجهة بتبحث عنه في `../../Voting_System` نسبةً لفولدرها.

للتشغيل مباشرةً من الباك:

```powershell
cd 'E:\Git_Hub Projects\Voting_System'
npm run start:local
```

افتح [المشروع المحلي](http://127.0.0.1:3000). السيرفر بيعرض الواجهة والـAPI مع بعض. التشغيل مش محتاج `npm install` أو MySQL أو SMTP أو تعديل ملف `.env`.

لو مكان الواجهة اتغير أو البورت مش متاح، حدد القيم قبل التشغيل:

```powershell
$env:FRONTEND_DIR = 'E:\path\to\An-Enhanced-Web-Based-Electronic-Voting-System'
$env:LOCAL_PORT = '3001'
npm run start:local
```

افتح عنوان السيرفر، وليس `index.html` عن طريق `file://` أو Live Server. الـAPI موجود على نفس العنوان تحت `/api`.

## حسابات التجربة

| الحساب | الإيميل | الباسورد |
|---|---|---|
| ناخب | `voter@voting.local` | `Voter@12345` |
| أدمن | `admin@voting.local` | `Admin@12345` |

صفحة الدخول فيها أزرار لملء الحسابات دي. الحسابات خاصة بالتجربة المحلية فقط.

## الوظائف المكتملة

- تسجيل حساب مع فحص الاسم والإيميل وتاريخ الميلاد والموبايل ورقم تجريبي من 14 رقمًا، وعدم تكرار الإيميل أو الرقم.
- تحقق من الحساب بكود وإعادة إرسال كود جديد؛ ينتهي بعد 10 دقائق ويقفل بعد 5 محاولات خاطئة.
- دخول وخروج بجلسة على السيرفر وملف cookie من نوع HttpOnly.
- نسيان الباسورد: طلب كود ← تحقق ← كلمة مرور جديدة. الكود لا يستخدم مرتين والجلسات القديمة تنتهي.
- تعديل الاسم والموبايل والصورة؛ باقي بيانات الهوية للقراءة فقط.
- استعراض وبحث وتصفية الانتخابات الحالية والقادمة والمنتهية.
- قراءة المرشحين واختيار العدد المسموح ومراجعة الاختيارات، ثم التصويت مرة واحدة فقط.
- سجل التصويت مع الوقت ورقم إيصال والاختيارات.
- نتائج بعد الإغلاق: الأصوات والنسب والترتيب والتعادل وحالة صفر أصوات وتنزيل CSV.
- إدارة الانتخابات: مسودات، إضافة وتعديل وحذف مرشحين وصورهم، نشر الانتخابات، وإغلاق التصويت وإظهار النتائج.
- إرسال ملاحظات وقراءتها من حساب الأدمن.
- الروابط القديمة للـ24 صفحة تفتح الواجهة الجديدة؛ تفاصيل الانتخابات موجودة في الرابط.

## البيانات الحقيقية مقابل التجريبية

**الانتخابات الأولية والأسماء وحسابات التجربة والأصوات الموجودة في الانتخابات المنتهية بيانات تجريبية.** أي حساب أو تصويت أو تعديل جديد يتم حفظه فعليًا محليًا في `Voting_System/data/voting-demo.sqlite`، ويفضل موجودًا بعد إعادة التشغيل. ملف قاعدة البيانات متجاهَل في Git. احتفظ بنسخة احتياطية بعد إيقاف السيرفر؛ حذف الملف يعني فقدان بيانات التجربة.

**الإيميل مش بيتبعت فعليًا**: الكود بيظهر على الشاشة. **الهوية مش مربوطة بسجل رسمي**: استخدم بيانات وهمية، والفحص للصيغة وعدم التكرار فقط. سن التسجيل الأدنى 16 قاعدة للتجربة الحالية وليس إقرارًا بمتطلبات انتخابات حقيقية.

السيرفر مربوط بـ`127.0.0.1` فقط. لا تنشره للعامة أو عبر tunnel؛ الأكواد والحسابات التجريبية ظاهرة عمدًا. النسخة عرض تخرج عملي وليست جاهزة لانتخابات رسمية. سجل التصويت يربط الاختيارات بالحساب ولا ينفذ بروتوكول اقتراع سري.

كود NestJS/MySQL الأصلي موجود في `Voting_System/src` و`prisma`. الواجهة الجديدة مرتبطة بعقد API السيرفر المحلي، ولا تتوافق مباشرة مع مسارات Nest القديمة. لم يتم الاتصال بقاعدة MySQL القديمة أو تغيير بياناتها. دمج الإنتاج يحتاج تخزينًا وعقد API متوافقين، وخدمة إيميل، وسجل أهلية رسمي، ومراجعة خصوصية الاقتراع.

## دورة الانتخابات

1. أنشئ مسودة؛ تظهر للأدمن فقط.
2. أضف مرشحين بعدد لا يقل عن أقصى عدد اختيارات.
3. انشر بعد مراجعة المواعيد والمرشحين. بعد النشر لا يمكن تعديلهم أو حذف الانتخابات.
4. التصويت يفتح ويغلق حسب الوقت. يمكن للأدمن إغلاق انتخابات مفتوحة مبكرًا بعد التأكيد.
5. النتائج تظهر بعد الإغلاق فقط. النسب = أصوات المرشح ÷ إجمالي اختيارات المرشحين، وليست نسبة المشاركة. مع صفر أصوات لا يوجد فائز؛ ومع التعادل يظهر ترتيب مشترك.

## الاختبارات وتنظيم الملفات

في فولدر الواجهة شغل `npm run check`. في فولدر الباك شغل `npm run test:local`.

الاختبارات تستخدم قواعد منفصلة ولا تغيّر بيانات المستخدم. تشمل الصلاحيات، التسجيل والتحقق، الجلسات، استرجاع الباسورد، الأصوات المتزامنة، النتائج، وثبات البيانات بعد إعادة التشغيل.

- `js/app.js`: الصفحات والتوجيه والتفاعلات.
- `js/api.js`: الاتصال بالسيرفر والأخطاء وتنسيق البيانات.
- `css/app.css`: التصميم والاستجابة للشاشات المختلفة.
- `scripts/sync-pages.mjs`: توحيد صفحات HTML كنقاط دخول للتطبيق.
- ملفات JavaScript وCSS القديمة متروكة كمرجع، لكن صفحات HTML لا تحملها.

لا توجد شبكة خطوط أو مكتبات واجهة خارجية لازمة للتشغيل.
